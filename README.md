# YourClean - Система калькулятора для клининговой компании

Полнофункциональный Django проект для клининговой компании с калькулятором стоимости услуг.

## Особенности

- 🧮 Интеллектуальный калькулятор с пошаговым процессом
- 🧹 Два типа услуг: Клининг и Химчистка
- 💰 Гибкая система расчёта цен с коэффициентами
- 📅 Система расписания и бронирования
- 🌍 Мультиязычность (RU, CZ, EN)
- 📊 Интеграция с Google Sheets
- 🔔 Уведомления владельцу о новых заказах
- ⚙️ Полная админка для управления всеми параметрами

## Установка

1. Клонируйте репозиторий или создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта:
```
SECRET_KEY=your-secret-key-here
GOOGLE_SHEETS_CREDENTIALS_FILE=path/to/credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com
OWNER_EMAIL=owner-email@gmail.com
```

4. Выполните миграции:
```bash
python manage.py makemigrations
python manage.py migrate
```

5. Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

6. Загрузите переводы:
```bash
python manage.py compilemessages
```

7. Запустите сервер:
```bash
python manage.py runserver
```

## Настройка админки

После входа в админку (`/admin/`) настройте:

1. **Услуги** - создайте услуги "Клининг" (code: `cleaning`) и "Химчистка" (code: `dry_cleaning`)
2. **Типы уборки** - поддерживающая, генеральная, после ремонта (укажите цену за м²)
3. **Периодичность** - разовая, раз в месяц, раз в 2 недели, раз в неделю (укажите коэффициенты скидки)
4. **Сложность** - недавно, давно, очень давно (укажите коэффициенты сложности)
5. **Дополнительные услуги** - любые дополнительные услуги с ценами
6. **Объекты химчистки** - диван, кресло, матрас, ковёр и т.д.
7. **Расписание** - создайте доступные слоты для записи

## Структура калькулятора

### Экран 1: Выбор типа услуги
- Клининг
- Химчистка

### Экран 2: Параметры услуги

**Для клининга:**
- Площадь (м²)
- Тип уборки
- Сложность (когда последний раз была генеральная)
- Периодичность
- Дополнительные услуги

**Для химчистки:**
- Выбор объектов

### Экран 3: Результат расчёта
- Итоговая цена
- Детали заказа
- Кнопка "Оформить заказ"

### Экран 4: Оформление заказа
- Выбор даты и времени
- Контактные данные
- Комментарий

## Логика расчёта

### Клининг:
```
база = площадь × цена за м²
база × коэффициент сложности
результат × коэффициент скидки
+ доп. услуги
= итог
```

### Химчистка:
```
сумма цен всех выбранных объектов = итог
```

## Интеграция с Google Sheets

1. Создайте проект в Google Cloud Console
2. Включите Google Sheets API
3. Создайте сервисный аккаунт и скачайте JSON ключ
4. Сохраните файл как `credentials.json`
5. Укажите путь в `.env`: `GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json`
6. Создайте Google таблицу и поделитесь с email сервисного аккаунта
7. Укажите ID таблицы в `.env`: `GOOGLE_SHEETS_SPREADSHEET_ID=...`

## Мультиязычность

Проект использует `django-parler` для перевода контента. Все модели с переводами можно редактировать в админке на разных языках.

Для добавления переводов в шаблонах используйте:
```django
{% load i18n %}
{% trans "Текст для перевода" %}
```

## Лицензия

Проект создан для YourClean.


# 🗄️ Настройка базы данных PostgreSQL на Render

## Проблема
Админка не работает, возможно из-за проблем с базой данных.

## ✅ Решение: Создание PostgreSQL базы данных

### Шаг 1: Создайте базу данных в Render

1. В Render Dashboard нажмите **"New +"**
2. Выберите **"PostgreSQL"**
3. Настройки:
   - **Name:** `yourclean-db` (или любое имя)
   - **Database:** `yourclean` (или любое имя)
   - **User:** `yourclean_user` (или любое имя)
   - **Plan:** Free
4. Нажмите **"Create Database"**

### Шаг 2: Подключите базу данных к сервису

1. Откройте ваш Web Service в Render Dashboard
2. Перейдите в раздел **"Environment"**
3. Найдите переменную `DATABASE_URL` - Render должен автоматически добавить её при создании базы данных
4. Если переменной нет, добавьте её вручную:
   - **Key:** `DATABASE_URL`
   - **Value:** (скопируйте из настроек базы данных)

### Шаг 3: Проверьте настройки в render.yaml

В файле `render.yaml` уже настроена база данных:
```yaml
database:
  name: yourclean-db
  plan: free
  databaseName: yourclean
  user: yourclean_user
```

Если вы создали базу данных с другими именами, обновите `render.yaml` или используйте переменную `DATABASE_URL` напрямую.

### Шаг 4: Перезапустите сервис

После создания базы данных:
1. Render автоматически добавит `DATABASE_URL` в Environment Variables
2. Перезапустите Web Service
3. При запуске выполнятся миграции и создастся суперпользователь

## 🔍 Проверка работы базы данных

### Через логи Render

После перезапуска проверьте логи:
1. Render Dashboard → Ваш сервис → **Logs**
2. Должны быть сообщения:
   - `[OK] Подключение к базе данных успешно`
   - `[OK] Суперпользователь "admin" успешно создан`

### Если видите ошибки:

**"relation does not exist"** → Миграции не выполнены
- Решение: Убедитесь, что `python manage.py migrate` выполняется в Start Command

**"could not connect to server"** → База данных не подключена
- Решение: Проверьте `DATABASE_URL` в Environment Variables

**"password authentication failed"** → Неправильные учетные данные
- Решение: Проверьте настройки базы данных в Render

## 📋 Чеклист настройки базы данных

- [ ] PostgreSQL база данных создана в Render
- [ ] База данных подключена к Web Service (через `render.yaml` или вручную)
- [ ] Переменная `DATABASE_URL` установлена в Environment Variables
- [ ] Web Service перезапущен
- [ ] Миграции выполнены (проверьте логи)
- [ ] Суперпользователь создан (проверьте логи)
- [ ] Админка работает (проверьте вход)

## 🔧 Альтернатива: Использование DATABASE_URL напрямую

Если `render.yaml` не работает, можно настроить вручную:

1. В Render Dashboard → Ваш сервис → Environment
2. Добавьте переменную:
   - **Key:** `DATABASE_URL`
   - **Value:** (скопируйте из настроек PostgreSQL базы данных)

Формат обычно такой:
```
postgresql://user:password@host:port/database
```

Render автоматически создаст эту переменную при подключении базы данных к сервису.

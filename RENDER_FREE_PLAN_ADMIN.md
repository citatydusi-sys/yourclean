# 🆓 Создание админа на бесплатном плане Render (без Shell)

## Проблема
На бесплатном плане Render Shell может быть недоступен или не работать.

## ✅ Решение: Автоматическое создание суперпользователя

Создан скрипт `create_superuser.py`, который автоматически создает суперпользователя при каждом запуске сервиса (если его еще нет).

## 🔧 Настройка в Render Dashboard

### 1. Environment Variables (Переменные окружения)

В Render Dashboard → Ваш сервис → Environment добавьте:

- `ADMIN_USERNAME` = `admin` (или ваш логин)
- `ADMIN_EMAIL` = `admin@yourclean.cz` (или ваш email)
- `ADMIN_PASSWORD` = `ваш_пароль` (⚠️ ВАЖНО: измените на безопасный пароль!)

### 2. Start Command уже обновлен

В `render.yaml` Start Command уже включает создание суперпользователя:
```bash
python manage.py migrate && python create_superuser.py && gunicorn yourclean.wsgi:application --bind 0.0.0.0:$PORT
```

## 📝 Как это работает

1. При каждом запуске сервиса выполняется `create_superuser.py`
2. Скрипт проверяет, существует ли пользователь с указанным логином
3. Если нет - создает суперпользователя с данными из Environment Variables
4. Если уже существует - пропускает создание

## 🔐 Безопасность

**ВАЖНО:** 
- После первого входа в админку **ОБЯЗАТЕЛЬНО** измените пароль!
- Не используйте простые пароли в продакшене
- Пароль хранится в Environment Variables - не коммитьте его в код!

## 🚀 После деплоя

1. Дождитесь успешного деплоя
2. Откройте: `https://ваш-домен.onrender.com/admin/`
3. Войдите с данными из Environment Variables:
   - Логин: значение `ADMIN_USERNAME`
   - Пароль: значение `ADMIN_PASSWORD`
4. **Сразу измените пароль** в админке!

## 🔄 Если нужно изменить пароль

### Вариант 1: Через Environment Variables
1. Измените `ADMIN_PASSWORD` в Render Dashboard
2. Перезапустите сервис
3. Удалите старого пользователя через админку (если нужно)
4. При следующем запуске создастся новый с новым паролем

### Вариант 2: Через админку
1. Войдите в админку
2. Users → выберите пользователя
3. Измените пароль и сохраните

## ⚠️ Если админка все еще не работает

Проверьте:
1. ✅ `DEBUG=False` в Environment Variables
2. ✅ `ALLOWED_HOSTS` содержит ваш домен Render
3. ✅ Миграции выполнены (проверьте логи)
4. ✅ Суперпользователь создан (проверьте логи - должно быть `[OK] Суперпользователь создан`)

## 📋 Чеклист

- [ ] `ADMIN_USERNAME` установлен в Environment Variables
- [ ] `ADMIN_EMAIL` установлен в Environment Variables  
- [ ] `ADMIN_PASSWORD` установлен в Environment Variables (безопасный пароль!)
- [ ] Код обновлен (с `create_superuser.py`)
- [ ] Сервис перезапущен
- [ ] Проверен вход в админку
- [ ] Пароль изменен после первого входа

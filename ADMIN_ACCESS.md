# 🔐 Доступ к админ панели Django

## URL админки

**Локально:** http://localhost:8000/admin/  
**На Render:** https://yourclean.onrender.com/admin/

## Создание нового суперпользователя

Если у вас еще нет доступа, создайте суперпользователя:

```bash
python manage.py createsuperuser
```

Вам будет предложено ввести:
- **Username** (логин)
- **Email address** (опционально)
- **Password** (пароль - будет скрыт при вводе)
- **Password (again)** (подтверждение пароля)

## Сброс пароля существующего пользователя

Если забыли пароль, можно сбросить его:

```bash
python manage.py changepassword username
```

Замените `username` на ваш логин.

## Создание суперпользователя через скрипт

Если нужно создать пользователя программно:

```bash
python manage.py shell
```

Затем в Python shell:
```python
from django.contrib.auth import get_user_model
User = get_user_model()
User.objects.create_superuser('admin', 'admin@example.com', 'your_password')
```

## Проверка существующих суперпользователей

```bash
python manage.py shell -c "from django.contrib.auth import get_user_model; User = get_user_model(); [print(f'Логин: {u.username}, Email: {u.email}') for u in User.objects.filter(is_superuser=True)]"
```

## Важно

- Логин и пароль чувствительны к регистру
- После создания суперпользователя войдите в админку по адресу `/admin/`
- Если проект на Render, используйте Render Shell для выполнения команд
